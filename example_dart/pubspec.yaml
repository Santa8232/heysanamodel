name: example_dart
description: Pure Dart CLI client to test and evaluate the HeySanaModel Manipur Tourism AI recommendation system.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.2.0
  path: ^1.9.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
